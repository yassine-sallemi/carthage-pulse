apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # This is the path INSIDE the Grafana container where it will look for JSONs
      path: /etc/grafana/provisioning/dashboards
